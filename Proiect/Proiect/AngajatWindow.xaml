<Window x:Class="Proiect.AngajatWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Proiect"
        mc:Ignorable="d"
        Title="Pagina Angajat"
        Height="550"
        Width="800"
        WindowStartupLocation="CenterScreen">

    <Grid Background="#F5F7FA" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="WelcomeTextBlock"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBox x:Name="SearchStudentTextBox"
                     Width="250"
                     Height="35"
                     Margin="0,0,15,0"/>

            <Button Content="Caută student"
                    Width="130"
                    Height="35"
                    Margin="0,0,15,0"
                    Click="SearchStudentButton_Click"/>

            <Button Content="Arată toți"
                    Width="120"
                    Height="35"
                    Click="ShowAllStudentsButton_Click"/>
        </StackPanel>

        <DataGrid x:Name="StudentsDataGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  MouseDoubleClick="StudentsDataGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Username student" Binding="{Binding Username}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="3" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Content="Deschide fișa studentului"
                    Width="190"
                    Height="35"
                    HorizontalAlignment="Left"
                    Click="OpenStudentFileButton_Click"/>

            <Button Grid.Column="1"
                    Content="Logout"
                    Width="100"
                    Height="35"
                    Click="LogoutButton_Click"/>
        </Grid>
    </Grid>
</Window>